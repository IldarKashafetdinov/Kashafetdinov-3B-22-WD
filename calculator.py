def get_summ(a, b):
    return (f"{a} + {b} = {a + b}")

def get_substraction(a, b):
    return (f"{a} - {b} = {a - b}")

def get_multiplication(a, b):
    return (f"{a} * {b} = {a * b}")

def get_division(a, b):
    return (f"{a} / {b} = {a / b}")
